## behat install in macOS ##

 - composer install 
 	 - curl -sS https://getcomposer.org/installer | php
	 - php composer.phar
	 - php composer.phar require --dev behat/behat
	 - vendor/bin/behat --init
 - check version
     - vendor/bin/behat -V

